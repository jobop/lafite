{namespaceStmt}:
namespace {nameSpaceName};

List{constStmt}:
   const {constName}:={constValue}

List{functionStmt}:
func {funcName}({params...}) returns({ret...}){

List{varStmt}:
   var {varName}:={varValue}
   var {varName}:={varValue}
   var {varName}:={varValue}
   ...


List{constStmt}:
   const {constName}:={constValue}
   const {constName}:={constValue}
   const {constName}:={constValue}
   const {constName}:={constValue}
   ...

List{ifStmt}:
    if {ifStmt}{

    }
    ...


List{forStmt}:
    for(){
    }
    ...

}